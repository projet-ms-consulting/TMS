<form method="post" action="{{ path('admin_address_delete', {'id': address.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimé cette addresse ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ address.id) }}">
    <button class="btn">Delete</button>
</form>
