{% extends 'admin.html.twig' %}

{% block title %}New School{% endblock %}

{% block body %}
    <h1>Create new School</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">Create</button>
    {{ form_end(form) }}
    <a href="{{ path('admin_address_new') }}">Créer une nouvelle addresse</a>
    <a href="{{ path('admin_school_index') }}">back to list</a>
{% endblock %}
