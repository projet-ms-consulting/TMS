{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

    <section>
                <div class="text-center">
                    <h1 class="h2">
                        Bienvenue sur TMS <br>
                        <span class="h1">Partout et à tout moment</span>
                    </h1>

                    <h2 class="h3">
                        <p> Notre application est conçue pour simplifier <br> la gestion des stagiaires et des entreprises partenaires.</p>
                        <p>Explorez nos fonctionnalités avancées pour gérer efficacement vos données.</p>
                    </h2>

                    <div class="flex flex-col items-center justify-center gap-2 mx-auto mt-8 md:flex-row">
                        {% if app.user %}
                            <a href="{{ path('app_profil') }}">
                                <button class="inline-flex items-center justify-center w-full h-12 gap-3 px-5 py-3 font-medium text-white duration-200 bg-gold md:w-auto rounded-xl hover:bg-gray-700 focus:ring-2 focus:ring-offset-2 focus:ring-black" aria-label="Mon Profil">
                                    Mon Profil
                                </button>
                            </a>
                        {% else %}
                            <a href="{{ path('app_login') }}">
                                <button class="inline-flex items-center justify-center w-full h-12 gap-3 px-5 py-3 font-medium text-white duration-200 bg-gray-900 md:w-auto rounded-xl hover:bg-gray-700 focus:ring-2 focus:ring-offset-2 focus:ring-black" aria-label="Connexion">
                                    Connexion
                                </button>
                            </a>
                        {% endif %}
                    </div>
                </div>
    </section>
{% endblock %}