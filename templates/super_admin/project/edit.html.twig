{% extends 'super_admin.html.twig' %}

{% block title %}Modifier le projet{% endblock %}

{% block body %}
    <h1>Modification du projet</h1>

    {{ form_start(form) }}
    {{ form_widget(name) }}
    {{ form_row(description) }}
    {{ form_row(participant) }}
    {{ form_row(link) }}
    {{ form_end }}

    <a href="{{ path('super_admin_project_index') }}">Retour</a>

    {{ include('super_admin/project/_delete_form.html.twig') }}
{% endblock %}
