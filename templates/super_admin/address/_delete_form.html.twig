<form method="post" action="{{ path('super_admin_address_delete', {'id': address.id}) }}" onsubmit="return confirm('Êtes vous sur de vouloir supprimer cette adresse ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ address.id) }}">
    <button class="btn">Supprimer</button>
</form>
