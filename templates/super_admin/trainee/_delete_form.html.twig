<form method="post" action="{{ path('super_admin_app_trainee_delete', {'id': personne.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer ce stagiaire ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personne.id) }}">
    <button class="delete-cv-btn">Supprimer</button>
</form>
