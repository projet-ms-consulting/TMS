<form method="post" action="{{ path('super_admin_app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="delete-cv-btn">Supprimer l'utilisateur</button>
</form>
